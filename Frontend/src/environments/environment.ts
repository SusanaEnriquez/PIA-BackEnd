// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyDVu0H6LC_t-X8QpFqB95D4dhAXvKW0k3U',
    authDomain: 'base-labam.firebaseapp.com',
    databaseURL: 'https://base-labam.firebaseio.com',
    projectId: 'base-labam',
    storageBucket: 'base-labam.appspot.com',
    messagingSenderId: '376867776443',
    appId: '1:376867776443:web:0620d96e75467566e5c69e'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
